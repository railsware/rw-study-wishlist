<% if @reservations == nil %>
    <h3>У вас не зарезервированных желаний</h3>
<% else %>
    <div id="container">
      <% @reservations.each do |r| %>
          <%= link_to image_tag(r.wish.image, :border => 0, :class => 'item'), '#' %>
      <% end %>
    </div>
<% end %>
